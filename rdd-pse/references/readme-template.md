# README 模板与编写规范

> 本文档定义了 README.md 的标准结构和编写要求。

---

## 标准结构

```markdown
# [项目名称]

[一句话描述 — 让读者 5 秒内理解项目用途]

## [可选] 设计理念 / 背景

[为什么做这个项目？核心设计思想是什么？2-3 句话或一个 ASCII 流程图即可]

## 亮点 / 为什么用它

[3-5 条场景化亮点。每条结构：先点出痛点，再给出本项目的解法，最后用证据（代码位置 / 机制 / 数据）兜底]

- **[痛点场景]** → [本项目怎么解决]（证据：`path/to/file` 的 [具体机制]）
- **[痛点场景]** → [本项目怎么解决]（证据：...）

## 快速开始

### 前置依赖

- [Node.js >= 18]
- [Rust >= 1.80]
- [Docker]

### 安装

```

```
git clone [url]
cd [project]
npm install
```

### 运行

```

```
npm run dev
```

打开 http://localhost:3000

## 项目结构

```

```
项目根
├── src/         — 主源码
│   ├── components/  — UI 组件
│   ├── services/    — API 服务
│   └── utils/       — 工具函数
├── tests/       — 测试代码
├── docs/        — 文档
└── config/      — 配置文件
```

## 使用方式

### 常用命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 构建生产版本 |
| `npm test` | 运行测试 |
| `npm run lint` | 代码检查 |

### [如有多模式或命令参数，在此展开]

## 技术栈

| 层级 | 技术 |
|------|------|
| 语言 | TypeScript |
| 前端框架 | React 18 |
| 构建工具 | Vite |
| 测试 | Vitest |
| Lint | ESLint + Prettier |

## [可选] 贡献指南

[如何参与开发、分支命名规范、提交规范等]
```

---

## 各节编写要点

### 项目名称与描述

- **名称**：用代码中的实际项目名（`package.json` 的 `name` 字段）
- **描述**：一句话，不要超过 30 字。聚焦"解决什么问题"
- 反面示例："一个功能强大的企业级微服务架构解决方案"（太空泛）
- 正面示例："基于 RDD 工作流的 AI 辅助开发 skill 集合"（具体）

### 亮点 / 为什么用它

- **场景化叙述**：每条亮点写成一个"痛点 → 解法"的小场景，让读者先共情（"以前这事多烦"）再看解法
- **证据兜底**：每条亮点结尾必须挂一个证据——代码位置（`file:line`）、配置项或可复现机制。没证据的删掉
- **限 3-5 条**：卖点超过 5 个等于没卖点。从候选清单里挑冲击力最强的，其余舍弃
- **先痛点后解法**：每条先说"没有它会怎样"，再说"本项目怎么治"，顺序反了就没冲击力
- 凑不够 3 条有证据的亮点就如实少写，不硬凑功能凑数

### 快速开始

- **必须能在 3 步内跑起来**：clone → install → run
- 如有环境变量，给出 `.env.example` 路径和必填项
- 如有 Docker，优先给 Docker Compose 命令

### 项目结构

- 只列顶层目录和关键子目录，不要枚举到第 3 层
- 每个目录后面写 2-5 字的说明
- 如果项目复杂，可以按"前端" / "后端" / "公共"分组

### 使用方式

- 优先用表格列常用命令
- 如果功能较多，按场景分组（开发 / 构建 / 部署 / 测试）
- 如果有 CLI 工具，给出常用参数示例

### 技术栈

- 用表格分类（语言 / 框架 / 构建 / 测试 / 数据库 等）
- 标注版本号（如在配置文件中已有）或只写名称

### 贡献指南

- 如果项目有 `.github/CONTRIBUTING.md`，在此链接过去
- PSE 不自动创建贡献指南，除非用户明确要求

---

## 更新已有 README 的流程

1. 完整读取现有 `README.md`，逐节记下内容
2. 执行项目分析流程（`project-analysis.md`），对比现状
3. 标注差异：

   ```
   待更新内容：
   ┌──────────────────┬──────────────────────────────────────┐
   │ 节               │ 变化                                  │
   ├──────────────────┼──────────────────────────────────────┤
   │ 快速开始          │ 新增 Docker 部署方式                  │
   │ 项目结构          │ 新增 rdd-pse/ 目录                   │
   │ 技术栈            │ 测试框架从 Jest 迁移到 Vitest         │
   └──────────────────┴──────────────────────────────────────┘
   ```

4. 用户确认哪些差异需要更新后，逐节修改
5. 保留已有的有效内容，不过度重写

---

## 风格要求

- **用中文写**（除非项目本身是全英文的）
- **终端友好**：代码块标注语言，路径用反引号
- **用 ASCII 而不是 emoji**，保持 README 在任何终端都能正常渲染
- **避免无证据的形容词**：不要写"高性能"、"易扩展"这类没有具体证据的词；但有证据的特色**必须呈现**——见"亮点"章节的"痛点→解法→证据"结构
- **链接优先**：如已有详细文档，用链接引用而不是重复内容
- **先功能后原理**：让读者先知道怎么用，再了解为什么这样设计
