# Final Report — openrca-bank-task6-1800-orchestrated-001

- 案例:`openrca-bank-task6-1800`(L2 批量,Bank/2021_03_04/task_6 [18:00-18:30])
- 臂型:orchestrated(2 轮,5 worker,5/5 有效回调)
- 终态:**hypothesis_space_exhausted**(可检验假设空间穷尽,排除法裁决;第 3/4 轮保留)

## 1. 根因结论(评测口径,置信度 0.6)

> **根因组件:Redis02(宿主 OS 层)**
> **原因:high memory usage**——匿名内存单采样阶跃 +5.4GB(UserMem 1668→7054MB,NoCacheMemPerc 92.6%),零 CPU/零 IO
> **时刻:起点 ∈(T+540, T+600]**,持续 ~5 采样(至 T+840),释放后进入低于原基线的新稳态

**裁决依据(排除法 + 唯一事件型异常)**:症状窗口 [0,+1800] 内全系统唯一的独占性事件型异常(Redis01 及全部其余宿主同期无变化);所有其他候选被证据排除(见 §2)。该事件起点与 OpenRCA 真值时刻(Redis02 high memory usage @18:09=T+540)一致。

来源定性(观测面允许的最大程度):零 CPU/零 IO 的纯匿名内存占用,内存画像(UserMem≈7050MB/NoCache≈91%)与 Mysql01 常态几乎一致——疑似某 Mysql 量级进程短暂运行于 Redis02 宿主,但观测面无进程级指标,身份不可证。

## 2. 排除项

| 候选 | 关键反证 |
|------|----------|
| Mysql02 IO 争用(T−600 起缓变) | E4(0.62):台阶化平台无第二突变;**同等劣化与症状无共存**(劣化持续至 T+2400,应用 T+60 后正常 39min);DB 指标对事件窗 [T+20,T+35] 零成像;11 服务均匀停滞无剂量-响应 |
| Redis 实例进程异常 | E2(0.7):used_memory 恒 13-16MB/4GB、零 evicted/blocked/rejected、命中率/ops 正常 |
| 应用层实例 | E3(0.7):CPU/堆/GC/磁盘/网络错误全平稳;T+0 停滞为 Tomcat02/04 叶 span 阻塞的子树传导 |
| Redis02 宿主内存事件→T+0 尖峰因果 | E5(0.93):时序倒置 600s;事件期应用零影响(mrt 峰 791ms 为常态) |

## 3. 诚实缺口:T+0 metric_app 尖峰致因不可判定

T+0(=18:00)发生的 11 服务 mrt 尖峰(ST4 2755ms,cnt −30%,60s 恢复;trace 事件窗 [T+20,T+35],MG 叶 span 阻塞自身占比 100%)——其致因在观测面上**不可判定**:全部可检验候选排除后,剩余不可证伪空间为网络瞬态(观测面无 retrans/RTT 指标)与进程级行为(无进程指标)。log/trace 对 Mysql、Redis 均零覆盖为结构性盲区。

## 4. 与托管真值的关系

真值(Redis02 high memory usage @T+540)与评测口径结论**一致**(组件/原因/时刻命中)。但调查同时发现:该根因事件对应用面零影响,与 T+0 可观测尖峰无因果——评测集将"根因事件"与"症状表现"解耦标注的实例(比 pilot 更极端:时序倒置 600s)。此为 L2 重要观察(observations §7 累积第三例)。

## 5. 机械结构观察

- 排除法收敛路径首次走通:两轮内从 3 域 5 候选穷尽至唯一事件型异常;worker 自主使用"无症状共存"判据(同等劣化强度事件前后不产生症状→非触发器)与"画像指纹比对"(事件内存画像 vs Mysql01 常态)
- 事件起点判定:60s 采样下用"末基线点/首偏离点"夹逼 ∈(T+540,T+600],与真值 60s 粒度对齐
- 假设空间耗尽停止条件首次触发(预算余 2 轮主动保留)

---
*Manager(goal goal-631136f7),rounds 1-2;证据链 E1-E5;评分见 summary*
