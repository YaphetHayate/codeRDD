# 结案报告 — openrca-bank-task1-2100

**Run**: openrca-bank-task1-2100-ralph-001(L2 基线臂,单体模式)
**结案轮次**: 第 1 轮(预算 4 轮,提前达成停止规则 1)
**final_state**: root_cause_concluded

---

## 一、结论(评测口径作答)

| 项 | 值 |
|---|---|
| **根因发生时刻(相对 T0 秒)** | **∈ (+180s, +240s],点估计 t = +240s**(60s 采样粒度) |
| **根因发生时刻(绝对时间)** | **2021-03-04 21:04:00 UTC+8**(采样界定区间 21:03:00–21:04:00) |
| **根因组件** | **apache02** |
| **根因原因(辅助)** | apache 进程组 apache_10001 进程数 6→5:一个 worker 进程异常退出且持续不恢复(观测末端 t=+2400 仍为 5);主机未重启(uptime 连续)、无内存/CPU 资源压力,为进程级故障,退出诱因在本观测面不可见(本窗 log_service.csv 无数据) |
| **置信度** | 0.82(停止规则 1:≥0.8 且有 causal 证据) |

## 二、证据链(全部可追溯,详见 state/evidence-chain.jsonl)

**因果性证据(causal)**
- **E001**: `plane/metric_container.csv` cmdb_id=apache02, kpi=OSLinux-OSLinux_PROCESS_apache_10001_PROCPPCount:t=-3600..+180 恒 6.0,**t=+240..+2400 恒 5.0** —— 对故障组件状态的直接观测(阶跃型状态丢失,持续不恢复)。

**支撑证据(correlational)**
- **E002** 对照: apache01 同名 KPI 全窗恒 6.0 → 排除采集器伪影,定位单机。
- **E003** 排除主机级成因: 阶跃时刻 apache02 uptime 连续、内存 49% 平、CPU<0.5% → 进程级退出。
- **E004** 窗内穷尽唯一性: 18 主机 × 全部 KPI 序列 robust-z 扫描(基线=全追溯窗),[0,1800) 内唯一故障级持续偏离即 apache02 PROCPPCount(36 采样);其余旗标为零基线微抖/瞬态毛刺。
- **E005** 窗内唯一影响: t=269/272 单条 trace gw0120210304210428486553(起始 21:04:28)经 Tomcat03→MG01→IG01 同步停顿 5020–5028ms;窗内其余各分钟 span 最大 ≤~2.5s(1-2s 慢 span 为连续背景)。时序晚于进程死亡窗 28–88s。
- **E006(weak)**: dockerB1/B2 分钟 span 数 t=240–300 同步下探,z 仅 −1.0~−1.9,方向一致但不显著。
- **E009** 无服务级失败: 四台 Tomcat ErrorCount 全窗恒定(0/2/0/4);metric_app sr 全程 100,[0,1800] 内 rr/mrt 干净;log_service.csv 仅表头(40 字节)。

## 三、排除项清单(均凭证据剪枝,非凭空排除)

| 假设 | 排除依据 |
|---|---|
| H2 Mysql02 慢查询/锁风暴(≈t=-2820) | 风暴区间 [-2820,-540] 与追溯窗 app 尖峰一一对应,但 **t=0 前已完全恢复**,窗内 Mysql02 无偏离(E007/E004)——真实异常、非本案根因 |
| H3 docker 容器层故障(-3120 / -2220..-2040) | 停顿均在 t<0,t=-1560 起恢复;窗内 docker 无异常(E007/E006) |
| H4 IG01 JVM/主机 CPU 异常 | t=+240 时刻仍为基线,冲高仅在 360–600/720–1020 且瞬态恢复(E008) |
| H5 IG01 非堆内存泄漏 | NoHeapMemoryUsed 平坦微漂(826611440→826591424)(E008) |
| H6 Tomcat 主机内存压力 | MEMFreeMem 全窗线性漂移(含故障前 1h),无阶跃无饱和(E008) |
| H7 Redis02 异常 | 旗标为累计计数器伪异常且起始均 <0;窗内 Redis 无故障级偏离(E004) |

## 四、推理路径摘要
1. 症状窗 [0,1800] 内 app 指标、错误计数、成功率均干净,trace 亦无成规模停顿 → 故障为组件级单点事件。
2. 窗内穷尽扫描(全部组件 × 全部 KPI)唯一故障级持续状态变化 = apache02 进程数 6→5 @ t=+240;对照主机与阶跃时刻全 KPI 差分排除全局伪影与主机级成因 → 因果性定位(E001~E004)。
3. 窗内唯一影响信号(单 trace 三层 5.02s 停顿,t=269/272)在时序上晚于进程死亡窗口,构成一致的(相关性)影响佐证(E005/E006)。
4. 追溯窗两组大幅异常(Mysql02 慢查询风暴、docker 独立停顿)均于 t=0 前恢复,判为窗外噪声/既往事件,不与"窗内单次故障"冲突(E007)。

## 五、诚实声明(缺口与边界)
- **时刻精度**: 60s 指标采样只能界定根因时刻于 (+180s,+240s];若评测需单一时刻,取 **t=+240s(21:04:00)**。
- **传导机制**: apache 不在 trace 观测面内,apache02 进程退出 → 5.02s 停顿的传导路径为相关性推断;根因定位基于组件状态观测与穷尽唯一性,不依赖该传导。
- **退出诱因**: 进程为何退出(crash/OOM-killer 不可见/被杀)在现有观测面(指标+trace,本窗无日志)不可判定。

## 六、产物清单(全部位于本 run 目录)
- `state/evidence-chain.jsonl`(E001–E009,已 read-back 校验)
- `state/hypothesis-tree.json`(H0+H1..H7,含剪枝理由与结论)
- `state/round-log.jsonl`(第 1 轮决策快照)
- `report/rounds/round-01.md`(本轮人类复核快照)
- `trace_minute_agg.csv`(trace 按组件×分钟聚合的工作产物,1207 行)
- `manifest.json`(final_state=root_cause_concluded)
